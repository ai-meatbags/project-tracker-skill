# Project Patterns

Rules version: `1.0.0`
Updated: `YYYY-MM-DD`

## Назначение
Файл хранит только локальные технические правила этого проекта.

`tasks/_policies/arch-patterns.md` и `tasks/_policies/dev-plan.md` считаются upstream-managed и локально не редактируются в рамках выполнения задач.

## Формат правила
Для каждого правила:
- `PP-###`
- `Добавлено`
- `Статус` (`active|archived|obsolete`)
- `Приоритет` (`high|medium|low`)
- `Описание`
- `Требование`

Для потенциального переноса в upstream добавлять строку:
- `Upstream: candidate`

## Активные правила
- (Пока нет. Добавляйте только правила, подтвержденные в этом проекте.)

## Архив
- (Пока нет.)

## Как обновлять
1. Повысить `Rules version` при изменении набора правил.
2. Обновить `Updated`.
3. Для нового правила добавить `PP-###` и обязательные поля.
4. Если правило устарело — выставить `obsolete` и указать причину.
